
/*
document.getElementById("form_specialization").addEventListener("submit", function (e) { 
  selectedSpecialization = document.getElementById("selectSpecialization").value;
  console.log(selectedSpecialization);
      $(document).ready(function(){
        $.ajax({
          url: './sighupWizard.php',
          type: 'GET',
          data: {
            selectedSpecialization: selectedSpecialization
          }
        });
      });
  });
*/
/* 
window.addEventListener("load", ()=> {
  const loader = document.querySelector(".laoder");
  loader.classList.add("loader-hidden");

  loader.addEventListener("transitionend",()=>{
      document.body.removeChild("loader");
  })
});
*/